<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnlanguages">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ColorsActivity"
            android:label="Colors" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".FamilyActivity"
            android:label="Family members" android:parentActivityName=".MainActivity"/>
        <activity android:name=".PhrasesActivity"
            android:label="Phrases" android:parentActivityName=".MainActivity"/>
        <activity android:name=".NumberActivity"
            android:label="Numbers" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>